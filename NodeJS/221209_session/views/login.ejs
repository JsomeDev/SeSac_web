<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8" />
  <meta http-equiv="X-UA-Compatible" content="IE=edge" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <script src="https://cdn.jsdelivr.net/npm/axios/dist/axios.min.js"></script>
  <title>Home</title>
</head>

<body>
  <h5>로그인을 환영합니다.</h5>
  <button type="button" onclick="logout()" class="logout-btn">로그아웃</button>


  <script>
    function logout() {
      axios({
        method: "DELETE",
        url: "/user",
      }).then((res) => {
        if (res.data) {
         alert("로그아웃 되셨습니다!");
         location.href="/"
        } else alert("알 수 없는 오류가 발생하였습니다. 다시 한번 시도해주세요.")
      });
    }
  </script>
</body>

</html>